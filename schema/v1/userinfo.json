{
   "$schema": "http://json-schema.org/draft-07/schema#",
   "title": "UserInfo",
   "$id": "#userinfo",
   "description": "Information about the logged in users",
   "type": "object",
   "properties": {
      "LoggedIn": {
         "description": "Amount of logged in users",
         "type": "integer",
         "minimum": 0
      }
   },
   "required": ["LoggedIn"]
}